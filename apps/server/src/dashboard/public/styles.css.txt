:root {
	color-scheme: dark;
	font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
	font-synthesis: none;
	--bg: #090a0b;
	--surface: #101214;
	--surface-raised: #15181b;
	--border: #272b30;
	--border-strong: #3a4047;
	--text: #f4f5f6;
	--muted: #90979f;
	--subtle: #69717a;
	--accent: #dce5ed;
	--success: #69d69b;
	--warning: #e9b968;
	--danger: #ff8989;
	--blue: #7ab7ff;
}

* { box-sizing: border-box; }
html, body { min-height: 100%; margin: 0; background: var(--bg); color: var(--text); }
body { min-width: 320px; }
button, input, select, textarea { font: inherit; }
button { cursor: pointer; }
.hidden { display: none !important; }
.sr-only { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; }
.muted { margin: 0; color: var(--muted); line-height: 1.55; }
.eyebrow { margin: 0 0 7px; color: var(--muted); font-size: 12px; font-weight: 750; letter-spacing: .11em; text-transform: uppercase; }

.login-screen { display: grid; min-height: 100dvh; place-items: center; padding: 24px; background: radial-gradient(circle at 50% 15%, #1a1d20 0, var(--bg) 46%); }
.login-card { width: min(100%, 430px); padding: 34px; border: 1px solid var(--border); border-radius: 20px; background: rgba(16, 18, 20, .96); box-shadow: 0 32px 90px #0009; }
.login-card h1 { margin: 13px 0 8px; font-size: 30px; letter-spacing: -.04em; }
.login-card label, .field label, .field-label { display: block; margin: 20px 0 8px; color: #cbd0d5; font-size: 13px; font-weight: 650; }
.brand-mark { display: grid; width: 50px; height: 50px; place-items: center; border: 1px solid #484f57; border-radius: 14px; background: linear-gradient(145deg, #24292e, #101214); color: white; font-size: 23px; font-weight: 800; box-shadow: inset 0 1px #ffffff13; }
.brand-mark.small { width: 37px; height: 37px; border-radius: 11px; font-size: 16px; }

input, select, textarea { width: 100%; border: 1px solid var(--border); border-radius: 10px; outline: none; background: #0b0d0f; color: var(--text); }
input, select { min-height: 44px; padding: 0 12px; }
textarea { min-height: 110px; padding: 12px; resize: vertical; font-family: ui-monospace, SFMono-Regular, Consolas, monospace; font-size: 13px; }
input:focus, select:focus, textarea:focus { border-color: #6a747e; box-shadow: 0 0 0 3px #7683911f; }
.form-error { min-height: 20px; margin: 10px 0; color: var(--danger); font-size: 13px; }

.button { min-height: 42px; padding: 0 15px; border: 1px solid var(--border-strong); border-radius: 10px; background: #171a1d; color: #e1e4e7; font-size: 13px; font-weight: 650; }
.button:hover, .button:focus-visible { border-color: #626c76; background: #202428; outline: none; }
.button:disabled { cursor: wait; opacity: .55; }
.button.primary { border-color: #dfe5ea; background: #edf1f4; color: #111315; }
.button.primary:hover { background: white; }
.button.danger { border-color: #704545; background: #291717; color: #ffb4b4; }
.button.subtle { background: transparent; }
.button.full { width: 100%; }
.icon-button { display: grid; width: 38px; height: 38px; place-items: center; padding: 0; border: 1px solid var(--border); border-radius: 9px; background: #121416; color: var(--muted); font-size: 20px; }

.app-shell { display: grid; min-height: 100dvh; grid-template-columns: 250px minmax(0, 1fr); }
.sidebar { position: sticky; top: 0; display: flex; height: 100dvh; flex-direction: column; border-right: 1px solid var(--border); background: #0c0e10; }
.sidebar-header { display: flex; align-items: center; gap: 12px; padding: 20px 18px; border-bottom: 1px solid var(--border); }
.sidebar-header strong, .sidebar-header span { display: block; }
.sidebar-header strong { font-size: 15px; }
.sidebar-header span { margin-top: 3px; color: var(--muted); font-size: 12px; }
.nav-list { display: flex; flex: 1; flex-direction: column; gap: 6px; padding: 21px 12px; }
.nav-label { margin: 0 9px 7px; color: var(--subtle); font-size: 11px; font-weight: 750; letter-spacing: .11em; }
.nav-item { display: grid; min-height: 46px; grid-template-columns: 25px minmax(0, 1fr) auto; align-items: center; gap: 8px; padding: 0 11px; border: 1px solid transparent; border-radius: 10px; background: transparent; color: #8e969e; text-align: left; }
.nav-item:hover { background: #14171a; color: #dce0e4; }
.nav-item.active { border-color: #30353a; background: #181b1e; color: white; }
.nav-index { color: var(--subtle); font-family: ui-monospace, monospace; font-size: 11px; }
.count { min-width: 25px; padding: 3px 7px; border-radius: 20px; background: #24282c; color: #bec4ca; font-size: 11px; text-align: center; }
.live-dot, .status-dot { display: inline-block; width: 8px; height: 8px; border-radius: 50%; background: #4f555b; }
.live-dot.online, .status-dot.online { background: var(--success); box-shadow: 0 0 0 3px #69d69b1a; }
.sidebar-footer { display: grid; gap: 15px; padding: 17px; border-top: 1px solid var(--border); }
.connection { display: flex; align-items: center; gap: 10px; }
.connection span, .connection strong { display: block; }
.connection div span { color: var(--muted); font-size: 11px; text-transform: uppercase; }
.connection strong { margin-top: 3px; font-size: 13px; }

.main-panel { min-width: 0; }
.page-header { display: flex; min-height: 108px; align-items: center; justify-content: space-between; gap: 20px; padding: 22px 30px; border-bottom: 1px solid var(--border); background: #0d0f11e8; backdrop-filter: blur(15px); }
.page-header h2 { margin: 0 0 4px; font-size: 25px; letter-spacing: -.035em; }
.page-header .muted { font-size: 13px; }
.page-actions { display: flex; gap: 9px; }
.content-scroll { padding: 27px 30px 45px; }

.summary-grid, .stats-grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 12px; margin-bottom: 18px; }
.summary-card { min-width: 0; padding: 18px; border: 1px solid var(--border); border-radius: 13px; background: var(--surface); }
.summary-card span { display: block; color: var(--muted); font-size: 13px; }
.summary-card strong { display: block; margin: 9px 0 4px; overflow: hidden; color: white; font-size: 27px; letter-spacing: -.04em; text-overflow: ellipsis; }
.summary-card small { color: var(--muted); font-size: 12px; }
.panel { border: 1px solid var(--border); border-radius: 15px; background: var(--surface); overflow: hidden; }
.panel-toolbar { display: flex; min-height: 76px; align-items: center; justify-content: space-between; gap: 20px; padding: 15px 18px; border-bottom: 1px solid var(--border); }
.panel h3 { margin: 0; font-size: 16px; letter-spacing: -.015em; }
.panel-toolbar .muted { margin-top: 4px; font-size: 13px; }
.filters { display: flex; align-items: center; gap: 8px; }
.filters select { width: auto; min-width: 135px; }
.search { position: relative; width: min(280px, 28vw); }
.search input { padding-right: 62px; }
.search kbd { position: absolute; top: 11px; right: 9px; padding: 3px 6px; border: 1px solid var(--border); border-radius: 5px; color: #6f7780; font: 10px ui-monospace, monospace; }

.records-grid { display: grid; grid-template-columns: minmax(0, 1fr); gap: 13px; padding: 16px; }
.record-card { min-width: 0; border: 1px solid var(--border); border-radius: 13px; background: #0d0f11; overflow: hidden; }
.record-header { display: flex; align-items: center; justify-content: space-between; gap: 14px; padding: 18px; }
.record-identity { display: flex; min-width: 0; align-items: center; gap: 11px; }
.avatar { display: grid; width: 42px; height: 42px; flex: 0 0 auto; place-items: center; border: 1px solid #343a40; border-radius: 11px; background: #181b1e; color: #dce1e5; font-size: 16px; font-weight: 750; }
.record-title { min-width: 0; }
.record-title strong, .record-title span { display: block; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.record-title strong { font-size: 16px; }
.record-title span { margin-top: 5px; color: var(--muted); font: 13px ui-monospace, monospace; }
.badges { display: flex; flex-wrap: wrap; justify-content: flex-end; gap: 5px; }
.badge { padding: 5px 8px; border: 1px solid #343a40; border-radius: 99px; color: #b8bfc5; font-size: 10px; font-weight: 750; letter-spacing: .05em; text-transform: uppercase; }
.badge.active { border-color: #335b45; color: var(--success); }
.badge.expired { border-color: #66512d; color: var(--warning); }
.badge.revoked { border-color: #653b3b; color: var(--danger); }
.license-key-row { display: flex; min-width: 0; align-items: center; justify-content: space-between; gap: 18px; padding: 14px 18px; border-top: 1px solid var(--border); background: #0b0d0f; }
.license-key-copy { display: grid; min-width: 0; gap: 4px; }
.license-key-label { color: var(--muted); font-size: 11px; font-weight: 700; letter-spacing: .06em; text-transform: uppercase; }
.license-key-copy code { overflow: hidden; color: #dfe4e8; font-size: 14px; text-overflow: ellipsis; white-space: nowrap; }
.license-key-note { color: var(--subtle); font-size: 12px; }
.record-metrics { display: grid; grid-template-columns: repeat(3, 1fr); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }
.metric { min-width: 0; padding: 15px 18px; border-right: 1px solid var(--border); }
.metric:last-child { border-right: 0; }
.metric span, .metric strong { display: block; }
.metric span { color: var(--muted); font-size: 11px; text-transform: uppercase; }
.metric strong { margin-top: 6px; overflow: hidden; font-size: 15px; text-overflow: ellipsis; }
.record-meta { min-height: 51px; padding: 15px 18px; color: var(--muted); font-size: 13px; }
.record-footer { display: flex; flex-wrap: wrap; align-items: center; justify-content: space-between; gap: 12px; padding: 14px 16px; border-top: 1px solid var(--border); }
.record-footer > span { color: var(--muted); font-size: 12px; }
.row-actions { display: flex; flex-wrap: wrap; justify-content: flex-end; gap: 7px; }
.row-action { min-height: 36px; padding: 0 11px; border: 1px solid var(--border); border-radius: 8px; background: #14171a; color: #c2c8cd; font-size: 12px; }
.row-action:hover { border-color: #565f68; color: white; }
.row-action.danger { color: #f4a0a0; }
.empty-state { display: grid; min-height: 260px; place-items: center; align-content: center; gap: 8px; color: var(--muted); }
.empty-state strong { color: #d3d7db; }
.empty-state span:last-child { font-size: 13px; }

.stats-filters { display: flex; align-items: center; justify-content: space-between; gap: 20px; margin-bottom: 18px; padding: 17px 19px; }
.stats-layout { display: grid; grid-template-columns: minmax(0, 1.65fr) minmax(320px, .85fr); gap: 14px; }
.chart-panel > header, .feed-panel > header { display: flex; min-height: 72px; align-items: center; justify-content: space-between; gap: 14px; padding: 14px 17px; border-bottom: 1px solid var(--border); }
.legend { display: flex; flex-wrap: wrap; gap: 12px; color: var(--muted); font-size: 12px; }
.legend span::before { display: inline-block; width: 7px; height: 7px; margin-right: 5px; border-radius: 2px; content: ""; }
.legend .activation::before { background: var(--blue); }
.legend .heartbeat::before { background: var(--success); }
.legend .usage::before { background: var(--warning); }
.chart { display: flex; height: 320px; align-items: stretch; gap: 4px; padding: 20px 16px 34px; overflow-x: auto; }
.chart-column { position: relative; display: flex; min-width: 17px; flex: 1; align-items: flex-end; justify-content: center; gap: 2px; border-bottom: 1px solid #30353a; }
.chart-bar { width: 28%; min-height: 1px; border-radius: 3px 3px 0 0; }
.chart-bar.activation { background: var(--blue); }
.chart-bar.heartbeat { background: var(--success); }
.chart-bar.usage { background: var(--warning); }
.chart-column time { position: absolute; right: 50%; bottom: -21px; color: var(--subtle); font-size: 10px; transform: translateX(50%) rotate(-35deg); transform-origin: center; white-space: nowrap; }
.stream-state { padding: 5px 9px; border: 1px solid var(--border); border-radius: 99px; color: var(--muted); font-size: 11px; }
.stream-state.online { border-color: #31543f; color: var(--success); }
.event-feed { max-height: 320px; overflow-y: auto; }
.event-row { display: grid; grid-template-columns: 9px minmax(0, 1fr); gap: 10px; padding: 13px 15px; border-bottom: 1px solid #202429; }
.event-row:last-child { border-bottom: 0; }
.event-marker { width: 8px; height: 8px; margin-top: 4px; border-radius: 50%; background: #77818a; }
.event-marker.success { background: var(--success); }
.event-marker.rejected, .event-marker.error { background: var(--danger); }
.event-copy strong, .event-copy span { display: block; }
.event-copy strong { font-size: 13px; font-weight: 650; }
.event-copy span { margin-top: 5px; color: var(--muted); font-size: 12px; line-height: 1.5; }

dialog { width: min(calc(100% - 32px), 650px); max-height: calc(100dvh - 32px); padding: 0; border: 1px solid var(--border-strong); border-radius: 17px; overflow: auto; background: var(--surface-raised); color: var(--text); box-shadow: 0 30px 100px #000c; }
dialog::backdrop { background: #000b; backdrop-filter: blur(5px); }
dialog .wide { width: 100%; }
#access-dialog { width: min(calc(100% - 32px), 1000px); }
.dialog-card { margin: 0; }
.dialog-card.compact { max-width: 500px; }
.dialog-header, .dialog-footer { display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 18px 20px; }
.dialog-header { border-bottom: 1px solid var(--border); }
.dialog-header h2 { margin: 0; font-size: 20px; }
.dialog-body { padding: 20px; }
.dialog-footer { justify-content: flex-end; border-top: 1px solid var(--border); }
.dialog-error { padding: 0 20px; }
.field-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 0 13px; }
.field.full { grid-column: 1 / -1; }
.field-hint { display: block; margin-top: 7px; color: var(--muted); font-size: 12px; line-height: 1.5; }
.metadata-editor, .metadata-rows { display: grid; gap: 8px; }
.metadata-columns, .metadata-row { display: grid; grid-template-columns: minmax(0, .8fr) minmax(0, 1.2fr) auto; gap: 8px; }
.metadata-columns { padding: 0 1px; color: var(--subtle); font-size: 10px; font-weight: 700; letter-spacing: .07em; text-transform: uppercase; }
.metadata-row { align-items: center; }
.metadata-row input { min-width: 0; }
.metadata-add { justify-self: start; }
.type-settings { margin-top: 15px; padding: 14px; border: 1px solid var(--border); border-radius: 11px; background: #0d0f11; }
.notice { margin: 0 0 17px; padding: 12px; border: 1px solid #5b4b2c; border-radius: 10px; background: #211b11; color: #dbc18e; font-size: 12px; line-height: 1.55; }
.secret-value { display: block; margin-top: 15px; padding: 15px; border: 1px solid var(--border); border-radius: 9px; overflow-wrap: anywhere; background: #090a0b; font: 14px/1.6 ui-monospace, monospace; }
.access-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 12px; }
.access-panel { min-width: 0; padding: 14px; border: 1px solid var(--border); border-radius: 11px; background: #0d0f11; }
.access-panel h3 { margin: 0 0 11px; font-size: 14px; }
.access-panel h4 { margin: 18px 0 7px; color: var(--muted); font-size: 11px; letter-spacing: .07em; text-transform: uppercase; }
.inline-form { display: grid; grid-template-columns: minmax(0, 1fr) minmax(90px, .35fr) auto; gap: 7px; }
.access-panel .inline-form { grid-template-columns: minmax(0, 1fr) auto; }
.identifier-list, .meter-list { display: grid; gap: 6px; }
.identifier-row, .meter-row { display: flex; min-width: 0; align-items: center; justify-content: space-between; gap: 8px; padding: 8px 9px; border: 1px solid #262a2e; border-radius: 8px; background: #101214; }
.identifier-row code { min-width: 0; overflow-wrap: anywhere; font-size: 12px; }
.identifier-row span, .meter-row span { color: var(--muted); font-size: 12px; }
.identifier-detail { display: grid; min-width: 0; gap: 4px; }
.meter-row { padding: 12px; }
.meter-row strong { font: 14px ui-monospace, monospace; }
.meter-list { margin-bottom: 14px; }
.toast { position: fixed; z-index: 30; right: 20px; bottom: 20px; max-width: min(380px, calc(100% - 32px)); padding: 12px 15px; border: 1px solid var(--border-strong); border-radius: 10px; background: #1b1e21; box-shadow: 0 15px 45px #0009; color: #e8ebed; font-size: 13px; opacity: 0; pointer-events: none; transform: translateY(7px); transition: opacity .14s, transform .14s; }
.toast.visible { opacity: 1; transform: translateY(0); }
.toast.error { border-color: #764141; color: #ffb1b1; }

@media (max-width: 1100px) {
	.summary-grid, .stats-grid { grid-template-columns: repeat(2, 1fr); }
	.stats-layout { grid-template-columns: 1fr; }
	.panel-toolbar { align-items: stretch; flex-direction: column; }
	.filters { width: 100%; flex-wrap: wrap; }
	.search { width: auto; flex: 1 1 260px; }
	.filters select { flex: 1 1 150px; }
}

@media (max-width: 760px) {
	.app-shell { display: block; }
	.sidebar { position: sticky; z-index: 10; height: auto; border-right: 0; border-bottom: 1px solid var(--border); }
	.sidebar-header { padding: 10px 13px; }
	.sidebar-header span { display: none; }
	.nav-list { flex-direction: row; padding: 7px 10px 10px; }
	.nav-label, .nav-index { display: none; }
	.nav-item { flex: 1; grid-template-columns: minmax(0, 1fr) auto; }
	.sidebar-footer { display: none; }
	.page-header { align-items: flex-start; flex-direction: column; padding: 20px 17px; }
	.page-actions, .page-actions .button { width: 100%; }
	.content-scroll { padding: 17px 14px 35px; }
	.panel-toolbar, .stats-filters { align-items: stretch; flex-direction: column; }
	.filters { width: 100%; flex-wrap: wrap; }
	.search { width: 100%; }
	.filters select { flex: 1; }
	.access-grid { grid-template-columns: 1fr; }
}

@media (max-width: 520px) {
	.login-screen { align-items: end; padding: 0; }
	.login-card { border-right: 0; border-bottom: 0; border-left: 0; border-radius: 20px 20px 0 0; }
	.summary-grid, .stats-grid { grid-template-columns: 1fr; }
	.record-header, .record-footer { align-items: stretch; flex-direction: column; }
	.badges, .row-actions { justify-content: flex-start; }
	.license-key-row { align-items: stretch; flex-direction: column; }
	.field-grid { grid-template-columns: 1fr; }
	.inline-form { grid-template-columns: 1fr; }
	.metadata-columns { display: none; }
	.metadata-row { grid-template-columns: 1fr; padding: 10px; border: 1px solid var(--border); border-radius: 10px; }
	.metadata-row .row-action { justify-self: start; }
	dialog { width: 100%; max-width: none; max-height: calc(100dvh - 10px); margin: auto 0 0; border-right: 0; border-bottom: 0; border-left: 0; border-radius: 18px 18px 0 0; }
}

@media (prefers-reduced-motion: reduce) {
	*, *::before, *::after { transition-duration: .01ms !important; }
}
